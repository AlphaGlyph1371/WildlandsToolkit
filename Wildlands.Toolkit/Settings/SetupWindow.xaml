<Window x:Class="Wildlands.Toolkit.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wildlands Toolkit — Setup" Height="290" Width="580"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource Background}"
        UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Where is Ghost Recon Wildlands installed?"
                   FontSize="17" Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="{StaticResource TextDim}"
                   Margin="0,0,0,16"
                   Text="The folder that contains GRW.exe and the .forge archives."/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" Grid.Column="0" TextChanged="Path_Changed" Margin="0,0,8,0"/>
            <Button Grid.Column="1" Content="Browse" Click="Browse_Click" Margin="0"/>
        </Grid>

        <TextBlock Grid.Row="3" x:Name="HintText" Margin="0,12,0,0" TextWrapping="Wrap"
                   Foreground="{StaticResource TextDim}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click"/>
            <Button x:Name="ContinueButton" Content="Continue" Click="Continue_Click"
                    IsEnabled="False" Margin="0"/>
        </StackPanel>
    </Grid>
</Window>
