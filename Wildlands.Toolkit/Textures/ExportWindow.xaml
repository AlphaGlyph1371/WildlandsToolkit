<Window x:Class="Wildlands.Toolkit.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save as" Height="310" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource Background}"
        UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="17" Margin="0,0,0,16"
                   TextTrimming="CharacterEllipsis"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Folder" VerticalAlignment="Center"
                       Foreground="{StaticResource TextDim}"/>
            <TextBox Grid.Column="1" x:Name="FolderBox" Margin="0,0,8,0" TextChanged="Options_Changed"/>
            <Button Grid.Column="2" Content="Browse" Click="Browse_Click" Margin="0"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center"
                       Foreground="{StaticResource TextDim}"/>
            <TextBox Grid.Column="1" x:Name="NameBox" TextChanged="Options_Changed"/>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Format" VerticalAlignment="Center"
                       Foreground="{StaticResource TextDim}"/>
            <ComboBox Grid.Column="1" x:Name="FormatBox" Margin="0,0,16,0"
                      SelectionChanged="Options_Changed"/>
            <TextBlock Grid.Column="2" Text="Levels" VerticalAlignment="Center"
                       Foreground="{StaticResource TextDim}"/>
            <ComboBox Grid.Column="3" x:Name="LevelBox" Margin="0" SelectionChanged="Options_Changed"/>
        </Grid>

        <TextBlock Grid.Row="4" x:Name="HintText" Margin="0,14,0,0" TextWrapping="Wrap"
                   FontFamily="Consolas" FontSize="12" Foreground="{StaticResource TextDim}"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True"/>
            <Button x:Name="SaveButton" Content="Save" Click="Save_Click" Margin="0" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
